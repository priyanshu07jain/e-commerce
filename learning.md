**backend**
make a backend folder
make a project using django called 'backend'
make a app called 'store'
add store to installed apps
pip install dotenv posgres
pip install django-cors-headers djangorestframework psycopg
add to middleware cors-headers
CORS_ALLOW_ALL_ORIGINS=[
    " http://localhost:5173/",
]
why related_name in models
why use casecade
why blank in textfield
what is differnce between imagefield and urlfield
what is serializers
what is rest_framework
what is django guardian
what is @api_view


**frontend**
create a vite app on frontend
